on:
  push:
    branches:
      - main
    paths: &paths
      - "config/**"
      - "zephyr/**"
      - "build.yaml"
  pull_request_target:
    branches:
      - main
    paths: *paths
    types:
      - opened
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3.0
